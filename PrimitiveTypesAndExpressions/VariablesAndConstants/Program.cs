using System;

namespace VariablesAndConstants
{
    class Program
    {
        static void Main(string[] args)
        {
            var a = 10;
            var b = 3;

            // When we have both numbers, in the operation, beeing integers, and the result should be a float number, it will only be shown as float with the values in the start are float.
            // a + b = 13;
            // a / b = 3;

            // If we would like to receive the result as floating number, we should cast it
            // c = ((float)a / (float)b);
        }
    }
}
